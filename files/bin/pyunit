#! /bin/python


import sys
import os, subprocess

def test_name(test):
	return "test_" + test + ".py"

args = [ test for test in "test_" + sys.argv + ".py" if os.path.exists(test_name(test))] 

subprocess.Popen(["./test.sh"] + args)

