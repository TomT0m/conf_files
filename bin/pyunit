#! /usr/bin/python


import sys
import os, subprocess

def test_name(test):
	return "test_" + test + ".py"

args = [ test_name(test) for test in sys.argv if os.path.exists(test_name(test))] 

proc = subprocess.Popen(["./test.sh"] + args)

proc.wait()

